services:
  - type: web
    name: allfoodsicily-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: starter  # Required for always-on service

